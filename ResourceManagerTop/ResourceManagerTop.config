#This file is read at buildtime
set(autostart true)
set(preconditions platform)
set(callsign "org.rdk.ResourceManagerTop")

if(PLUGIN_TOP_STARTUPORDER)
set (startuporder ${PLUGIN_TOP_STARTUPORDER})
endif()

