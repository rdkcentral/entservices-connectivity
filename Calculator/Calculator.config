# Renamed from Calc.config for naming consistency.
# ...existing configuration content...
set (autostart false)
set (preconditions Platform)
set (callsign "org.rdk.Calculator")

if(PLUGIN_CALCULATOR_STARTUPORDER)
set (startuporder ${PLUGIN_CALCULATOR_STARTUPORDER})
endif()
